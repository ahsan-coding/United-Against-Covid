# list array
list = [1, 6, 10, "Some string", True, False, 6]
# mutable and can have duplicates

# tuple array
tuple = (1, 4, 10, 29, 10, 4, "Some string")
# tuples are immutable (can't be changed after being defined or created) but can have duplicates

# set array
set = {"some string", 10, "more string", True}
# immutable and can't have duplicates
